'use client';

/**
 * @fileOverview Empty Browse page placeholder.
 */

export default function BrowsePage() {
  return (
    <main className="min-h-[calc(100vh-4rem)] flex flex-col items-center justify-center p-4">
      {/* Empty page as requested */}
    </main>
  );
}
