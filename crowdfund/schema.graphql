enum CampaignStatus {
  Active
  Successful
  Failed
}

type Campaign @entity {
  id: ID!
  owner: Bytes!
  title: String!
  target: BigInt!
  deadline: BigInt!
  amountCollectedUsd: BigInt!
  ethRaised: BigInt!
  withdrawn: Boolean!
  status: CampaignStatus!
}
